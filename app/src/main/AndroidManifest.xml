<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="udinsi.dev.bookcollection">

    <application
        android:name=".BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="udinsi.dev.wallpaper.Wallpaper">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DetailBookActivity" />
        <!-- <activity android:name=".uiux.Ui" /> -->
        <activity android:name=".uiux.Ux" />
        <activity android:name=".uiux.Ui" />
        <activity android:name=".rxandroid.RxLatihan" />
        <activity android:name=".rxandroid.RxLatihan2" />
    </application>

</manifest>